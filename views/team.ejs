<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Express-brain</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="shortcut icon" href="/images/favicon.ico" type="image/x-icon">
</head>
<body>
    <%- include('partials/nav') %>

    <main class="container mx-auto px-4 py-8">
        <div class="max-w-3xl mx-auto text-center space-y-6">
            <% if (team) { %>
                <h1 class="text-4xl font-bold text-gray-800 mb-4">
                    <%= team.name %>
                </h1>
                <div class="bg-white shadow-md rounded-lg">
                    <table class="min-w-full">
                        <thead class="bg-gray-50">
                            <tr>
                                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase ">Meilleur Score</th>
                                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase ">Nom</th>
                                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase ">Email</th>
                                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase ">Admin</th>
                            </tr>
                        </thead>
                        <tbody class="bg-white divide-y divide-gray-200">
                            <% team.users.forEach(user => { %>
                                <tr>
                                    <td class="px-6 py-4 text-left text-sm font-medium text-gray-900"><%= user.highscore %></td>
                                    <td class="px-6 py-4 text-left text-sm font-medium text-gray-900"><%= user.name %></td>
                                    <td class="px-6 py-4 text-left text-sm font-medium text-gray-900"><%= user.email %></td>
                                    <td class="px-6 py-4 text-left text-sm font-medium text-gray-900"><%= user.admin %></td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            <% } else { %>
                <h1 class="text-4xl font-bold text-gray-800 mb-4">
                    Vous n'avez pas encore d'équipe
                </h1>
            <% } %>
        </div>
    </main>

    <%- include('partials/footer') %>
</body>
</html>