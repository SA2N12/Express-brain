<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Express-brain</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="shortcut icon" href="/images/favicon.ico" type="image/x-icon">
</head>
<body>

    <%- include('partials/nav') %>

    <main class="container mx-auto px-4 py-8">
        <div class="max-w-3xl mx-auto text-center space-y-6">
            <h1 class="text-4xl font-bold text-gray-800 mb-4">
                Express-brain
            </h1>

            <h2 class="text-xl text-gray-600 leading-relaxed">
                Découvrez en un minimum de tour le nombre qui se cache derrière la carte mystère
            </h2>

            <div class="mt-8 bg-white p-8 rounded-lg shadow-lg max-w-md mx-auto text-center">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-16 w-16 mx-auto mb-4 text-blue-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.228 9c.549-1.165 2.03-2 3.772-2 2.21 0 4 1.343 4 3 0 1.4-1.278 2.575-3.006 2.907-.542.104-.994.54-.994 1.093m0 3h.01M12 21a9 9 0 100-18 9 9 0 000 18z" />
                </svg>
                <h2 class="text-2xl font-bold text-gray-800 mb-4">Qui suis-je ?</h2>
                <h3 class="text-lg text-gray-600 mb-6">Quelle nombre se cache derrière la carte mystère</h3>
                <p class="text-lg font-semibold text-gray-700 bg-gray-100 rounded-full px-6 py-2 inline-block shadow-sm border border-gray-200 mb-6">
                    Essais : <span class="text-blue-600 font-bold"><%= tries %></span>
                </p>
                <form action="/jouer" method="post">
                    <input type="number" 
                    class="w-full px-4 py-2 mb-4 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
                    placeholder="Entrez votre réponse"
                    name="userNumber">
                    <button class="bg-blue-600 text-white px-6 py-2 rounded-md hover:bg-blue-700 transition duration-300"
                            type="submit">
                            J'ai trouvé
                    </button>
                </form>
                <% if (result && result.msg){ %>
                    <div class="mx-auto mt-4 p-4 rounded-md text-center
                        <% if (result.type === 'warning'){ %>
                                bg-yellow-100 text-yellow-700 border border-yellow-400
                        <% }else if (result.type === 'success'){ %>
                                bg-green-100 text-green-700 border border-green-400
                        <% }else if (result.type === 'error'){ %>
                                bg-red-100 text-red-700 border border-red-400
                        <% } %>">
                        <%= result.msg %>
                    </div>
                <% } %>
                <% if (result.type === 'success'){ %>
                    <a href="/reset" class="block mt-4 text-blue-600 hover:underline">Rejouer</a>
                <% } %>
            </div>
        </div>
    </main>

    <%- include('partials/footer') %>

</body>
</html>